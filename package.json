{
  "name": "ben-mitchell-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "check": "npm run test && npm run build",
    "worker:dev": "npx --yes wrangler dev --config worker/wrangler.jsonc",
    "worker:deploy": "npx --yes wrangler deploy --config worker/wrangler.jsonc",
    "spotify:authorize": "node scripts/spotify-authorize.mjs"
  },
  "dependencies": {
    "ogl": "1.0.11"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
